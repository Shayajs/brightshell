{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=20.19.0"
    },
    "scripts": {
        "build": "vite build",
        "build:docker": "docker run --rm -v \"$PWD\":/app -w /app node:22-alpine sh -c \"npm ci && npm run build\"",
        "build:docker:quick": "docker run --rm -v \"$PWD\":/app -w /app node:22-alpine sh -c \"npm run build\"",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^8.0.0"
    }
}

# À coller dans Nginx Proxy Manager → Proxy Hosts → brightshell → onglet « Advanced »
# (ou appliquer en relançant ../gateway/init-npm-hosts.sh après dev-up)
#
# Sans cette directive, le gateway impose souvent ~1 Mo et renvoie 413 avant brightshell_dev_nginx.

client_max_body_size 25m;
